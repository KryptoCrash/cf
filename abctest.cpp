#include <bits/stdc++.h>
using namespace std;

using ll = long long;
using pi = pair<ll, ll>;
using vi = vector<ll>;

int main() {
    int samples[10] = {1, 3, 5, 1, 4, 3, 0, 0, 0, 1};

    for (int i = 0; i < 10; i++) {
        cout << samples[i] << endl;
    }

    return 0;
}